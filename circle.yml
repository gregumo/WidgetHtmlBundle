machine:
  timezone:
    Europe/Paris
  hosts:
    fr.victoire.io: 127.0.0.1
    en.victoire.io: 127.0.0.1
  services:
    - redis
  php:
    version: 7.0.4

dependencies:
  pre:
    - composer install
    - cp vendor/victoire/victoire/circle.yml circle.yml